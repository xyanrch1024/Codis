# Codis Server 多线程模型

## 线程拓扑

```
┌──────────────────────────────────────────────────────────────────┐
│                        Server 进程                                │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  主线程 (1)                                                       │
│  └─ server.listen("127.0.0.1", port) 阻塞, cpp-httplib 内部 I/O   │
│                                                                  │
│  HTTP 工作线程 (N, cpp-httplib 按请求创建)                          │
│  ├─ handle_health/info/chat → 同步返回 JSON                       │
│  ├─ handle_acp()             → fire-and-forget, detach LLM        │
│  ├─ handle_acp_switch()      → conn_id 切换 session               │
│  └─ handle_acp_ws()          → WS 握手, 派生 sender/reader        │
│                                                                  │
│  WS sender 线程 (每连接 1)                                        │
│  └─ queue->pop() → ws.send() → 客户端                             │
│                                                                  │
│  WS reader 线程 (每连接 1, 同 HTTP 线程)                           │
│  └─ ws.read() 阻塞 → 断开时 cleanup_connection()                  │
│                                                                  │
│  LLM Worker 线程 (M, detach)                                     │
│  └─ run_acp_loop_broadcast() → LLM stream → broadcast(queue)     │
│                                                                  │
│  信号线程 (1)                                                      │
│  └─ std::signal → SIGINT/SIGTERM → server.stop()                 │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

## 线程职责

### 1. 主线程
- 阻塞在 `httplib::Server::listen()`
- cpp-httplib 内部使用 epoll/select 接受新连接
- 每个 HTTP 请求分配工作线程

### 2. HTTP 工作线程
- `handle_health()` / `handle_info()`: 同步返回 JSON
- `handle_chat()`: 同步调用 LLM，返回完整结果
- `handle_acp()`: fire-and-forget
  - 保存 user 消息到 SQLite
  - 检查 `sessions_[id].processing`
  - 空闲 → `detach` LLM worker 线程
  - 忙碌 → 请求入 `sessions_[id].pending` 队列，当前轮结束后自动补跑
  - 返回 202 Accepted
- `handle_acp_ws()`: WebSocket 长连接
  - 分配 `conn_id`，注册到 `sessions_[sid].conns[conn_id]`
  - 推送 `connected` 帧
  - 派生 sender 线程（负责 queue → ws.send）
  - 当前线程进入 `ws.read()` 阻塞循环检测断开

### 3. WS sender 线程
- 每连接一个，由 `handle_acp_ws` 派生
- 阻塞在 `queue->pop()`（condition_variable.wait）
- 收到帧 → `ws.send()` → TCP → 客户端
- 队列被关闭（pop 返回空）→ `ws.close()` 结束线程

### 4. LLM Worker 线程 (detach)
- `run_acp_loop_broadcast()`: 最多 10 轮 tool call
- 调用 `OpenAICompatibleProvider::stream_chat()`（同步阻塞 HTTP）
- 每收到 token → `broadcast(assistant/reasoning frame)` 到 `conns[conn_id]` 队列
- tool 执行完成 → `broadcast(tool_call/tool_result)`
- 循环结束 → `broadcast(done)` → 若有 `pending` 请求则用新请求补跑下一轮，否则 `processing = false`

### 5. 信号线程
- `std::signal` 注册 SIGINT/SIGTERM → 主线程条件变量唤醒 → `server.stop()`

## 锁模型

| 资源 | 锁类型 | 操作 | 竞争 |
|------|--------|------|------|
| **sessions_** map | `mutex` | 读写 conns/pending/processing | 低 |
| **FrameQueue** | `mutex` + `condition_variable` | push: lock → queue.push → cv.notify<br>pop: lock → cv.wait → queue.pop | 低 |
| **SessionStore** (SQLite) | `mutex` | 所有 SQL 操作串行化 | 中 |
| **Logger** | `mutex` | cerr/file 串行写入（设了 OPENCODE_LOG_FILE 时不写 stderr） | 低 |
| **ProviderRegistry** | `shared_mutex` | 读多写少 | 极低 |
| **ToolRegistry** | `shared_mutex` | 读多写少 | 极低 |

## 数据流

```
HTTP 线程 (handle_acp)
  │
  ├─ SessionStore::append_message()            ← mutex (SQLite)
  ├─ sessions_[sid].processing.exchange(true)
  │   ├─ 返回 true (已有 LLM) → pending.push_back(req) → 202
  │   └─ 返回 false → detach LLM Worker
  │
  └─ std::thread([...] { ... }).detach()
        │
        ▼
  LLM Worker 线程 (run_acp_loop_broadcast)
  │
  ├─ SystemContext::build_baseline()          ← SessionStore (mutex)
  ├─ OpenAICompatibleProvider::stream_chat()
  │   └─ LLMHttpClient::stream_post()         ← HTTPS POST (同步阻塞)
  │       └─ on_token → broadcast(frame)
  │           │
  │           └─ sessions_[sid].conns[conn_id]->push(frame)
  │                ← mutex + cv.notify_one
  │
  ├─ ToolRegistry::execute() → broadcast(tool_call/tool_result)
  └─ broadcast(done)
        ├─ pending 非空 → 取出 → detach 补跑下一轮 (保持 processing)
        └─ pending 空   → sessions_[sid].processing = false
              │
              ▼
  WS sender 线程 (每连接)
  │
  └─ queue->pop()                             ← cv.wait (阻塞)
      └─ ws.send(frame)                       ← TCP → Client
      └─ 队列关闭 → ws.close()                ← 连接结束
```

## 关键设计决策

### 1. SessionState 队列广播，而非 EventBus
EventBus 已废弃。LLM Worker 直接向 `sessions_[sid].conns[conn_id]` 队列 push 帧。
conn_id 为空时广播到该 session 的所有连接。多客户端下互不干扰。

### 2. broadcast 不直接写 TCP
LLM Worker 只入队，WS sender 线程负责 `ws.send()`。mutex + condition_variable
同步，push 端零等待、pop 端零 CPU 占用。

### 3. detach 而非 join
LLM 可能耗时 10+ 秒，HTTP 线程必须立即返回（202）。不做 join，不做 `future.get()`。

### 4. pending 排队而非静默丢弃
处理期间到达的消息入 `pending` 队列，当前轮 `done` 后按序补跑，不丢消息。
补跑时复用当前 run 的 conn_id（广播目标），下一轮完成后同样检查 pending。

### 5. processing 原子旗标
`atomic<bool> processing` + `exchange(true)` 保证只有第一个请求触发 LLM，
防止同 session 并发调用 LLM 导致重复收费。

### 6. SO_REUSEADDR 而非 SO_REUSEPORT
httplib 默认 `SO_REUSEPORT` 会让两个 server 进程同端口共存、连接被内核分流，
导致 WS 连接与 HTTP 请求落在不同进程、消息静默丢失。构造时覆盖为仅
`SO_REUSEADDR`，第二个实例 bind 直接失败并退出。
